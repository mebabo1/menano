          FILE2="programs/wineboot/wineboot.c"
          if grep -q 'initialize_xstate_features( data );' "$FILE2"; then
            echo "Removing xstate call for ARM64..."
            sed -i '/initialize_xstate_features( data );/d' "$FILE2"
            echo "✔ xstate call removed"
          else
            echo "✔ xstate already removed or not found"
          fi
